# Chargeback Shield Frontend Environment Template (frontend/.env.example)
# IMPORTANT: Frontend builds are public! NEVER expose API secret keys in VITE_ variables.

VITE_API_BASE_URL=http://localhost:8000
VITE_APP_ENV=production
